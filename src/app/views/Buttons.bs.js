// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Icon = require("./Icon.bs.js");
var React = require("react");
var ButtonStyles = require("./ButtonStyles.bs.js");

function getDisabled(state) {
  return state !== "Ready";
}

function getContent(state, children) {
  if (state === "Ready" || state === "Disabled") {
    return children;
  } else {
    return React.createElement(Icon.ProgressSpin.make, {
                color: "White"
              });
  }
}

function Buttons$Button(Props) {
  var state = Props.state;
  var onClick = Props.onClick;
  var colorOpt = Props.color;
  var sizeOpt = Props.size;
  var fullOpt = Props.full;
  var titleOpt = Props.title;
  var children = Props.children;
  var color = colorOpt !== undefined ? colorOpt : "Gray";
  var size = sizeOpt !== undefined ? sizeOpt : "Base";
  var full = fullOpt !== undefined ? fullOpt : false;
  var title = titleOpt !== undefined ? titleOpt : "";
  var className = ButtonStyles.makeClassName(state, color, size, full);
  return React.createElement("button", {
              className: className,
              title: title,
              disabled: state !== "Ready",
              type: "button",
              onClick: onClick
            }, getContent(state, children));
}

var Button = {
  make: Buttons$Button
};

function makeIcon(icon) {
  if (icon === "ChevronDown") {
    return React.createElement(Icon.ChevronDown.make, {
                color: "White"
              });
  } else if (icon === "ChevronUp") {
    return React.createElement(Icon.ChevronUp.make, {
                color: "White"
              });
  } else if (icon === "X") {
    return React.createElement(Icon.X.make, {
                color: "White"
              });
  } else if (icon === "ChevronDoubleUp") {
    return React.createElement(Icon.ChevronDoubleUp.make, {
                color: "White"
              });
  } else if (icon === "Trash") {
    return React.createElement(Icon.Trash.make, {
                color: "White"
              });
  } else if (icon === "Menu") {
    return React.createElement(Icon.Menu.make, {
                color: "White"
              });
  } else {
    return React.createElement(Icon.ChevronDoubleDown.make, {
                color: "White"
              });
  }
}

function Buttons$IconButton(Props) {
  var title = Props.title;
  var onClick = Props.onClick;
  var icon = Props.icon;
  var color = Props.color;
  var state = Props.state;
  return React.createElement(Buttons$Button, {
              state: state,
              onClick: onClick,
              color: color,
              size: "Small",
              title: title,
              children: makeIcon(icon)
            });
}

var IconButton = {
  makeIcon: makeIcon,
  make: Buttons$IconButton
};

exports.getDisabled = getDisabled;
exports.getContent = getContent;
exports.Button = Button;
exports.IconButton = IconButton;
/* Icon Not a pure module */
